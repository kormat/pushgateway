#!/usr/bin/make -f

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

%:
	dh $@ 

override_dh_auto_install:
	dh_install
	install -D debian/pushgateway.wrapper debian/pushgateway/usr/sbin/pushgateway

override_dh_auto_test:
