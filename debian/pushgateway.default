# Defaults for pushgateway initscript
# sourced by /etc/init.d/pushgateway
# installed at /etc/default/pushgateway by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_ARGS=""
